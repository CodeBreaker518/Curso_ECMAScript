const zoo = {
  lion: '🦁',
  cat: '🐱',
  panda: '🐼',
  fish: '🐟',
}

console.log(Object.keys(zoo))
// Output
// [ 'lion', 'cat', 'panda', 'fish' ]

// Object.keys() converts only the names of the Object into an array
